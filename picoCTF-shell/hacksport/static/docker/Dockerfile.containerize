FROM shellmanager/shellmanager

WORKDIR /src
COPY . /src

# TODO: ease exfil of artifacts and config
RUN shell_manager install /src \
    && shell_manager deploy all \
    && rm -rf /src

# TODO: default need to make explicit in standalone
EXPOSE 24399
CMD ["xinetd", "-dontfork"]
