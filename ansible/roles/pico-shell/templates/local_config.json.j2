{
    "hostname": "{{ shell_ip }}",
{% if enable_web_ssl | default(False) | bool -%}
    "web_server": "{{ web_address_internal_https }}",
{% else %}
    "web_server": "{{ web_address_internal }}",
{% endif %}
    "rate_limit_bypass_key": "{{ flask_app_rate_limit_bypass_key | default("INSECURE_DEFAULT_CHANGE_ME") }}"
}
